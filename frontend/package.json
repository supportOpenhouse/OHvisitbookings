{
  "name": "oh-visit-bookings",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "dev": "node --env-file=.env scripts/dev-server.mjs",
    "db:migrate": "node --env-file=.env scripts/migrate.mjs",
    "db:cleanup-test": "node --env-file=.env scripts/cleanup-test-rows.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4"
  }
}
